"use client";

const Client = () => {
  return (
    <div>
      <button onClick={() => alert("Are you sure to onClick here")}>
        Click Me
      </button>
    </div>
  );
};

export default Client;
